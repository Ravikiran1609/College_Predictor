# 1) Pin NumPy and pandas so they match ABIs
numpy==1.25.3
pandas==2.1.1

# 2) Then install tabula-py (which will pick up those pandas & numpy versions)
tabula-py==2.13.0

# 3) The rest of your FastAPI stack
fastapi==0.95.1
uvicorn[standard]==0.23.1
python-multipart==0.0.6

