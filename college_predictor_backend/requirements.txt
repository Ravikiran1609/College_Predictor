# 1) Pin NumPy and pandas to matching, available versions under Python 3.11
numpy==1.25.2
pandas==2.1.1

# 2) Then install tabula-py (which will pick up pandas & numpy)
tabula-py==2.13.0

# 3) The rest of your FastAPI stack
fastapi==0.95.1
uvicorn[standard]==0.23.1
python-multipart==0.0.6

